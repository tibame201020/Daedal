# Daedal Context

## 1. 問題的起點

生成式 AI 正在改變軟體開發。

現在的問題已經不是：

* AI 能不能寫程式
* AI 能不能理解需求

而是：

> AI 能否在長期專案中，穩定、可預測、可驗證地前進？

當 AI Agent 能連續產生數千行程式碼時，
失控的成本會被放大。

常見現象包括：

* 任務順序錯亂
* 狀態不一致
* 依賴破壞
* 合併污染主分支
* Scope Creep 擴散

這不是能力問題。
是治理問題。

---

## 2. 核心洞察

AI 需要的不是更多自由。

它需要：

* 明確邊界
* 狀態約束
* 可驗證推進
* 權力分離

Daedal 的核心假設是：

> 生成能力可以無限放大，但狀態推進必須被嚴格限制。

---

## 3. Daedal 的結構哲學

Daedal 將軟體工廠拆解為五個角色。

---

### 3.1 Daedal — 結構定義者

Daedal 是治理規則本身。

它定義：

* 任務如何拆分
* 任務如何依賴
* Worker 如何執行
* 狀態如何推進
* CI 如何仲裁

Daedal 不生成程式碼。
它定義生成的幾何結構。

---

### 3.2 Labyrinth — 專案結構

每個專案都是一座 Labyrinth。

它由以下組成：

* 任務 DAG（tracker.json）
* 規格藍圖（specs/*.yml）
* 仲裁流程（CI workflows）

Labyrinth 是靜態結構。

它不思考。
它只約束。

---

### 3.3 Ikaros — 執行者

Ikaros 是所有 Worker Agent 的統稱。

它們：

* 能生成程式碼
* 能修正錯誤
* 能進行有限自治

但它們不能：

* 跳過依賴鏈
* 推進未驗證狀態
* 繞過 CI

Ikaros 可以飛。
但飛行高度由結構決定。

---

### 3.4 Knossos — 標準調度層 (Standard Dispatching Layer)

Knossos 是 Daedal 生態系中的標準調度中心，也是**第一個完全基於 Daedal 框架開發的正式產物**。

在設計哲學上，Daedal 定義了靜態規則與結構，但框架有意識地留下了「誰來推動」的調度空洞。Knossos 作為 Daedal 技術棧的「旗艦示範作」，正是為點燃這個發動機而存在的標準解答。

*   **官方首選與自我實踐**：Knossos 本身的開發流程即是 Daedal 治理邏輯的最佳實踐證明。
*   **功能職責**：人機調度介面、PR 與 CI 的觀測中心、任務觸發引擎。
*   **與 Daedal 的關係**：如同 Kubernetes 與 nginx-ingress 的關係——它是「欽點」的標準解答，旨在向世人展示：透過 Daedal 的結構，我們可以建造出高度穩定的治理平台。

---

### 3.5 仲裁層 — CI/CD

CI 是物理法則。

*   測試失敗 → 不可合併
*   驗收未通過 → 不可推進
*   狀態不一致 → 阻止變更

在 Daedal 中：

> Merge 是唯一合法的狀態轉移。

---

## 4. 權力分離模型

Daedal 的核心設計是權力分離。

| 層級        | 職責 | 不可做 | 角色定位 |
| --------- | ---- | ----- | ------ |
| Daedal    | 定義規則 | 不生成代碼 | 憲法 / 治理邏輯 |
| Labyrinth | 承載結構 | 不決策 | 物理容器 / 迷宮 |
| Ikaros    | 執行任務 | 不裁決 | 工人 / 引擎 |
| Knossos   | **標準調度與觀測** | 不繞過 CI | **標準通訊官 (Optional)** |
| CI        | 仲裁 | 不生成 | 物理定律 |

### 4.1 無隱性依賴原則 (Zero-Implicit-Dependency)

Daedal 的底層邏輯嚴格遵守「無隱性依賴」。這意味著即使沒有 Knossos，框架仍能完美運作：

*   **Labyrinth 是自洽的**：`tracker.json` 是基於標準 `jq` 即可操作的純文字 JSON，不包含任何外部 Dashboard 的專有欄位。
*   **Arbitrator 是自治的**：任務合併與 Phase 推進由 GitHub Actions (CI) 執行，這屬於項目的「物理定律」，不依賴任何外部調度器。
*   **Ikaros 是去中心化的**：Worker 遵循的 `AGENT_PROTOCOL.md` 僅依賴於 Git 分支與 `tracker.json` 狀態，不依賴於 Knossos 的觸發指令。

Knossos 的存在是為了**極小化人類的維運負擔**，而非框架運行的前提。

Daedal 將常見反模式具象化為「Minotaur」。

Minotaur 包括：

* 巨型任務未拆分
* God Object
* 不可測試修改
* 依賴污染
* Scope 無節制擴張

系統透過：

* 微型任務拆分
* DAG 明確依賴
* CI 強制關卡

將 Minotaur 拆解為可治理單元。

---

## 6. 為何需要這種架構？

隨著 AI 能力提升，
單一 Agent 可能可以完成完整專案。

但能力提升不等於穩定性提升。

沒有治理的生成，
只會加速混亂。

Daedal 是對 AI 能力擴張的一種結構回應。

---

## 7. 未來方向

Daedal 的未來發展可能包括：

* 多 Worker 並行協作
* LLM 調度決策層
* 分散式狀態同步
* 自動化 Phase 管理
* 專案跨工廠協調

但無論如何擴充，
核心原則不變：

> 狀態推進必須可驗證。
> 生成必須受限於結構。

---

## 8. 總結

Daedal 並不是一個更聰明的 Agent。

它是一種讓 Agent 在長期專案中不失控的結構。

在生成式時代，
治理比生成更重要。

Daedal 的目標不是飛得更高。

而是確保每一次飛行都能安全降落。