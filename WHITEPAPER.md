# Daedal Context

## 1. 問題的起點

生成式 AI 正在改變軟體開發。

現在的問題已經不是：

* AI 能不能寫程式
* AI 能不能理解需求

而是：

> AI 能否在長期專案中，穩定、可預測、可驗證地前進？

當 AI Agent 能連續產生數千行程式碼時，
失控的成本會被放大。

常見現象包括：

* 任務順序錯亂
* 狀態不一致
* 依賴破壞
* 合併污染主分支
* Scope Creep 擴散

這不是能力問題。
是治理問題。

---

## 2. 核心洞察

AI 需要的不是更多自由。

它需要：

* 明確邊界
* 狀態約束
* 可驗證推進
* 權力分離

Daedal 的核心假設是：

> 生成能力可以無限放大，但狀態推進必須被嚴格限制。

---

## 3. Daedal 的結構哲學

Daedal 將軟體工廠拆解為五個角色。

---

### 3.1 Daedal — 結構定義者

Daedal 是治理規則本身。

它定義：

* 任務如何拆分
* 任務如何依賴
* Worker 如何執行
* 狀態如何推進
* CI 如何仲裁

Daedal 不生成程式碼。
它定義生成的幾何結構。

---

### 3.2 Labyrinth — 專案結構

每個專案都是一座 Labyrinth。

它由以下組成：

* 任務 DAG（tracker.json）
* 規格藍圖（specs/*.yml）
* 仲裁流程（CI workflows）

Labyrinth 是靜態結構。

它不思考。
它只約束。

---

### 3.3 Ikaros — 執行者

Ikaros 是所有 Worker Agent 的統稱。

它們：

* 能生成程式碼
* 能修正錯誤
* 能進行有限自治

但它們不能：

* 跳過依賴鏈
* 推進未驗證狀態
* 繞過 CI

Ikaros 可以飛。
但飛行高度由結構決定。

---

### 3.4 Knossos — 控制平面

Knossos 是觀測與觸發中心。

人類在此：

* 俯瞰整體狀態
* 檢視任務進度
* 查看 PR 與 CI
* 手動或排程觸發 Worker

Knossos 不負責裁決。
它只是視角與調度介面。

---

### 3.5 仲裁層 — CI/CD

CI 是物理法則。

* 測試失敗 → 不可合併
* 驗收未通過 → 不可推進
* 狀態不一致 → 阻止變更

在 Daedal 中：

> Merge 是唯一合法的狀態轉移。

---

## 4. 權力分離模型

Daedal 的核心設計是權力分離。

| 層級        | 職責    | 不可做    |
| --------- | ----- | ------ |
| Daedal    | 定義規則  | 不生成代碼  |
| Labyrinth | 承載結構  | 不決策    |
| Ikaros    | 執行任務  | 不裁決    |
| Knossos   | 觀測與觸發 | 不繞過 CI |
| CI        | 仲裁    | 不生成    |

沒有任何單一角色擁有完整權力。

---

## 5. 反模式與風險控制

Daedal 將常見反模式具象化為「Minotaur」。

Minotaur 包括：

* 巨型任務未拆分
* God Object
* 不可測試修改
* 依賴污染
* Scope 無節制擴張

系統透過：

* 微型任務拆分
* DAG 明確依賴
* CI 強制關卡

將 Minotaur 拆解為可治理單元。

---

## 6. 為何需要這種架構？

隨著 AI 能力提升，
單一 Agent 可能可以完成完整專案。

但能力提升不等於穩定性提升。

沒有治理的生成，
只會加速混亂。

Daedal 是對 AI 能力擴張的一種結構回應。

---

## 7. 未來方向

Daedal 的未來發展可能包括：

* 多 Worker 並行協作
* LLM 調度決策層
* 分散式狀態同步
* 自動化 Phase 管理
* 專案跨工廠協調

但無論如何擴充，
核心原則不變：

> 狀態推進必須可驗證。
> 生成必須受限於結構。

---

## 8. 總結

Daedal 並不是一個更聰明的 Agent。

它是一種讓 Agent 在長期專案中不失控的結構。

在生成式時代，
治理比生成更重要。

Daedal 的目標不是飛得更高。

而是確保每一次飛行都能安全降落。